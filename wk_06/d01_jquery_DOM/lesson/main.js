// Grab "customHeading" and change the text to "Slappy"

// Grab "specialText" and turn it green

// Add a 1px black border around each of the "contentCard" divs

// Grab the "uniqueImg" and set the src equal to a new image.

// Change the background color of contentBody to a light blue.

// Create a new class in CSS that is set up with a new font family from Google fonts. Programatically add that class to all h3 tags.

var corgis = []

// Finish the contructor below
var Corgi = function() {
  //name
  //heading
  //image
  //description
}

// Create 3 new corgis and add them to the corgis array

// Programatically recreate the 3 "contentCards" that you see on the DOM using these 3 new corgis.
