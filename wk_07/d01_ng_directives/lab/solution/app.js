var app = angular.module("ticketApp", [])

///////////////////////////////////////////////////////
/////////////////////// PROMPT ///////////////////////
/////////////////////////////////////////////////////

// Create a page that a user can use to add new tickets into the system for sale. Tickets should have the following attributes:
//  - ID
//  - Artist
//  - Date
//  - Price
//  - Section #
//  - Row #
//  - Seat #

// Create a Ticket Constructor in your controller and use that constructor to seed a "tickets" array with 6 new tickets.

// Don't forget to create $scope variables for each of your input fields so that we have access to user inputs in our controller.

// Create a click event so when a user is done inputting their ticket data they can submit it to the array.

// If they forget to input an Artist present them with some kind of error in the view.

// Create a ng-repeat so that we can see the tickets that are available for sale at the bottom of the page.

// Create 2 buttons on each ticket for Update and Delete.  Those ng-clicks should trigger those CRUD events.

// DON'T FORGET
// Add ng-app to your html tag
// Add ng-controller to your body tag
// Add script tags at the bottom of the <body> for all your JS files
