var app = angular.module("ticketApp", [])

///////////////////////////////////////////////////////
/////////////////////// PROMPT ///////////////////////
/////////////////////////////////////////////////////

// Filters can help us format our existing code as well as help ours users quickly find what they are looking for.
// Go to this link for Angular's documentation on filters  https://docs.angularjs.org/api/ng/filter

// Lets add a currency filter on each of our tickets
// Lets add a date filter on each of our tickets
// Lets add a uppdercase filter to each of the headings on our tickets
// Lets add a limitTo filter on our ng-repeat
// Lets add an orderBy filter on our ng-repeat
// Lets use the search field provided in conjunction with the "filter" feature that angular provides to allow our users to search for what they want.
